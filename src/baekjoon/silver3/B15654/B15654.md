# 백준 15654 - N과 M (5)

## 문제 설명
N개의 자연수가 주어졌을 때, 그 중 M개를 고른 수열을 중복 없이 사전 순으로 모두 출력하는 문제이다.

-----------------------------------

## 문제 풀이 과정

입력받은 배열을 `Arrays.sort()`로 오름차순 정렬해 사전 순 출력 조건을 만족시켰다.
`visited` 배열로 중복 선택을 방지하며, `ans` 배열에 선택된 수를 `depth` 인덱스로 저장했다.
`depth == m`이 되면 `ans`를 출력하고 반환했다.