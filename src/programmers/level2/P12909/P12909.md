# 프로그래머스 12909 - 올바른 괄호
## 문제 설명
문자열이 주어졌을 때, 괄호가 올바르게 짝지어져 있는지 확인하는 문제이다.
---
## 문제 풀이 과정
`Deque`를 스택으로 활용하여, `(`가 나오면 push하고 `)`가 나오면 pop하는 방식으로 풀었다. `)`가 나왔을 때 스택이 비어있으면 짝이 맞지 않으므로 즉시 `false`를 반환하도록 처리했다. 순회가 끝난 후 스택에 `(`가 남아있으면 짝이 맞지 않으므로 `false`를 반환한다.